#!/usr/bin/env bundle

source 'https://rubygems.org'

group :default do
  gem 'sass', '>= 3.2'

  gem 'sprockets', '~> 2.12.1'
end

group :development do
  gem 'rake', '~> 10.3.1'
end

group :development, :test do
  gem 'rspec', '~> 2.14.1'
end